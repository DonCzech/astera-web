import PickACardGame from "./PickACardGame";

export const metadata = {
  title: "Vyberte si kartu | Astera",
  description: "Zdarma výklad karet — vyberte si kartu a získejte vzkaz pro dnešní den.",
};

export default function PickACardPage() {
  return <PickACardGame />;
}
